node_modules/
dist/
build/
.next/
.turbo/
coverage/
pnpm-lock.yaml
*.tsbuildinfo

# Gerado e reescrito pelo Next.js a cada build — formatá-lo geraria diff eterno.
next-env.d.ts

# Relatórios do pipeline de agentes, definições de agentes e planejamento —
# texto editorial, não código. Não devem ser reformatados pelo Prettier.
# `AGENTS.md` e `.codex/` são o equivalente de `CLAUDE.md`/`.claude/` para o Codex.
.claude/
.codex/
docs/
sprint/
CLAUDE.md
AGENTS.md
