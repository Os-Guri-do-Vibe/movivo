node_modules/
dist/
build/
.next/
.turbo/
coverage/
pnpm-lock.yaml
*.tsbuildinfo

# Migrações e metadados gerados pelo drizzle-kit — reformatá-los brigaria com o gerador
# (o snapshot/journal é reescrito a cada `db:generate`). Não são código editável à mão.
apps/api/drizzle/

# Gerado e reescrito pelo Next.js a cada build — formatá-lo geraria diff eterno.
next-env.d.ts

# Relatórios do pipeline de agentes, definições de agentes e planejamento —
# texto editorial, não código. Não devem ser reformatados pelo Prettier.
# `AGENTS.md` e `.codex/` são o equivalente de `CLAUDE.md`/`.claude/` para o Codex.
.claude/
.codex/
docs/
sprint/
CLAUDE.md
AGENTS.md
