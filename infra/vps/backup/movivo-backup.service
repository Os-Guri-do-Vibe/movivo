[Unit]
Description=MOVIVO — backup diário cifrado (Postgres, EvolutionAPI, uploads)
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
User=deploy
Group=deploy
ExecStart=/opt/movivo/bin/movivo-backup.sh
Nice=10
IOSchedulingClass=idle
